---
import Layout from './Page.astro'
type Props = {
  frontmatter: {
    title?: string
    description?: string
  }
}

const { frontmatter } = Astro.props
---

<Layout {...frontmatter}>
  <div class="container mt-12 prose">
    <slot />
  </div>
</Layout>
